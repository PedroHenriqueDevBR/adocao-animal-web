import { AdoptionReceivedModel } from './adoption-received-model';

describe('AdoptionReceivedModel', () => {
  it('should create an instance', () => {
    expect(new AdoptionReceivedModel()).toBeTruthy();
  });
});
